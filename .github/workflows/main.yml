---
name: building a linux
on:
  push: null
  workflow_dispatch: null
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: basic linux commands
        run: |
          hostname -steps
          echo " hi to all"
          echo "ls -al"
